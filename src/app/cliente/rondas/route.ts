// Compatibility URL; both routes enforce the same verified session and scope.
export const dynamic = "force-dynamic";
export { GET } from "@/features/rail-queue/server/read";
export { POST } from "@/features/rail-queue/server/write";
